- name: apt update and upgrade Debian packages
  hosts: all
  become: true
  tasks:
    - name: Update package lists
      apt:
        update_cache: yes
    - name: Upgrade all packages
      apt:
        upgrade: dist
        autoremove: yes
        autoclean: yes
        purge: yes
